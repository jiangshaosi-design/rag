# RAG 智能体问答系统 — 答辩准备文档

## 一、技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| LLM | DeepSeek V4 Pro (langchain-openai) | 推理与生成 |
| Embedding | 阿里云 DashScope text-embedding-v4 (1024维) | 文本向量化 |
| 视觉模型 | 阿里云 Qwen-VL (dashscope MultiModalConversation) | 图片描述 |
| Agent 框架 | LangChain 1.3+ create_agent (ReAct模式) | 智能体编排 |
| 向量库 | FAISS (IndexIDMap + IndexFlatIP, 余弦相似度) | 向量检索 |
| 关系库 | MySQL 8.0 (InnoDB, utf8mb4) | 元数据与文本存储 |
| PDF 解析 | Docker MinerU (mineru[core], ~43GB镜像) | 版面分析 + 表格 + 图片分类 |
| 前端 | Streamlit / FastAPI + 原生 HTML/CSS/JS | 交互界面 |
| 分块 | RecursiveCharacterTextSplitter + 滑动窗口 | 文本切分 |

---

## 二、系统架构

```
用户界面 (Streamlit / HTML)
    │
    ▼
LangChain ReAct Agent (DeepSeek V4 Pro)
    ├─ Tool 1: check_history (对话缓存查询)
    ├─ Tool 2: search_knowledge_base (FAISS + MySQL)
    ├─ Tool 3: query_database (数据库查询)
    └─ Tool 4: manage_documents (文档管理)
    │
    ▼
数据层: FAISS (向量) + MySQL (元数据+文本) + File System (文档+图片)
```

---

## 三、文档入库全流程

### 流程概览
```
用户上传PDF → 保存 → Docker MinerU 解析 → 图片复制+描述 → 表格保护 → 分块 → 向量化 → FAISS + MySQL
```

### 步骤 1：文件保存与创建记录
**函数**: `pipeline.py:18 ingest_document()`
```python
doc = Document(filename=filename, file_type=file_type, file_path=file_path, ...)
doc_id = add_document(doc)  # MySQL documents 表插入，status=processing
```

### 步骤 2：MinerU Docker 版面分析
**函数**: `loader.py:23 PDFLoader.load()`
```python
# 启动 Docker 容器，挂载 PDF 目录和输出目录
cmd = ["docker", "run", "--gpus", "all", "--rm",
       "-v", f"{pdf_dir}:/input", "-v", f"{tmp_dir}:/output",
       "mineru:latest", "mineru", "-p", f"/input/{pdf}", "-o", "/output"]
proc = subprocess.Popen(cmd, stdout=subprocess.PIPE, ...)
for line in proc.stdout:  # 逐行解析 MinerU 进度条
    m = re.search(r'(\S.*?):\s*(\d+)%.*?(\d+)/(\d+)', line)
    # → 实时更新前端进度
```
MinerU 内部流程：版面分析(doclayout_yolo) → 文字提取(正确阅读顺序) → 表格检测 → 图片分类(自动过滤logo)

### 步骤 3：图片处理
**函数**: `loader.py:134` (图片处理段)
```python
# 复制图片到 data/images/
for fname in images:
    dst = os.path.join(settings.IMAGE_DIR, f"doc{doc_id}_{fname}")
    shutil.copy2(src, dst)

# Qwen-VL 并行描述（3线程, 20s超时, 可配置关闭）
if settings.IMAGE_DESCRIBE:
    with ThreadPoolExecutor(max_workers=3) as pool:
        for f in as_completed(futures):
            desc = f.result(timeout=20)

# 替换 markdown 引用: ![](images/xxx.jpg) → [IMAGE: docX_xxx.jpg]: 描述
md_text = re.sub(r'!\[.*?\]\(...\)', lambda _: new_tag, md_text)
```

### 步骤 4：表格保护
**函数**: `loader.py:250 _protect_markdown_tables()`
```python
# 自动检测 markdown 表格行（|...|格式），包裹保护标记
if is_table_line:
    result.append("[TABLE_START]")
    result.extend(table_buf)
    result.append("[TABLE_END]")
```

### 步骤 5：混合分块
**函数**: `splitter.py:68 split_text()`
```python
# 第1步：保护表格块 → 占位符替换
protected_text, placeholders = _protect_blocks(text)

# 第2步：递归分块（按段落/句子边界）
coarse_chunks = RecursiveCharacterTextSplitter(
    chunk_size=1000, chunk_overlap=100,
    separators=["\n\n", "\n", "。", "！", "？", ...]
).split_text(protected_text)

# 第3步：滑动窗口细分（超长块内部细化）
for coarse in coarse_chunks:
    if len(coarse) > window_size(500):
        result.extend(_apply_sliding_window(coarse, 500, 250))

# 第4步：还原表格块
return _restore_blocks(chunks, placeholders)
```

### 步骤 6：向量化
**函数**: `embedder.py:29 embed_batch()`
```python
# 每 10 条一批调用阿里云 API
for i in range(0, total, BATCH_LIMIT=10):
    batch = chunks[i : i+10]
    vectors = dashscope.TextEmbedding.call(
        model="text-embedding-v4", input=batch
    )
```

### 步骤 7：双存储
**函数**: `vector_store.py:76 add_to_index()` + `repository.py:74 add_chunks_batch()`
```python
# MySQL: 批量插入文本块，faiss_id = str(chunk_id)
add_chunks_batch([Chunk(document_id=..., content=..., faiss_id=str(cid)) for ...])

# FAISS: 向量归一化后存入 IndexIDMap
normalize(vectors)
index.add_with_ids(vectors, chunk_ids)
save_index(index)
```

---

## 四、问答全流程

### 流程概览
```
用户提问 → Agent 调用 check_history（缓存查询）→ 未命中 → search_knowledge_base → FAISS检索 → MySQL回查 → LLM生成回答 → 来源+图片展示
```

### 步骤 1：Agent 入口
**函数**: `agent.py:54 invoke_agent()`
```python
agent = get_agent()  # LangChain create_agent 单例
result = agent.invoke(
    {"messages": [("user", question)]},
    config={"recursion_limit": 25}
)
```

### 步骤 2：缓存查询（优先）
**函数**: `tools.py:118 check_history()`
```python
# 先用 SQL LIKE 粗筛（关键词匹配）
# 再用 Embedding 余弦相似度精排（>85% 阈值）
similar = search_similar_question(question, top_n=3)
if similar and similar[0]["similarity"] > 0.85:
    return cached_answer  # 直接返回缓存答案
```

### 步骤 3：知识库检索
**函数**: `tools.py:23 search_knowledge_base()`
```python
query_vec = embed_text(query)                     # 问题向量化
results = search(index, query_vec, top_k=5)       # FAISS Top-K 检索
chunks = get_chunks_by_faiss_ids(faiss_ids)       # MySQL 回查完整文本
# 返回格式化结果:
# [来源: CH32V103DS0.pdf | 块12] (相关度: 0.83)
# 文本内容...
```

### 步骤 4：来源提取与图片注入
**函数**: `agent.py:136` (来源提取段)
```python
for block in tool_content.split("\n---\n"):
    m = re.search(r'\[来源:\s*(.+?)\s*\|\s*块(\d+)\]\s*\(相关度:\s*([\d.]+)\)', block)
    sources.append({"filename": ..., "chunk_index": ..., "score": ..., "content": ...})
```

### 步骤 5：前端渲染
**函数**: `app.py:506` (对话渲染段)
```python
# 思考过程可折叠展示
with st.expander(f"🧠 思考过程 ({len(thinking)} 步)"):
    for step in thinking:
        if step["type"] == "thought": st.markdown(...)
        elif step["type"] == "action": st.markdown(...)
        elif step["type"] == "observation": st.markdown(...)

# 图片内嵌渲染
img_match = re.search(r'\[IMAGE:\s*(.+?)\]:\s*(.*)', content)
if img_match:
    st.image(os.path.join(settings.IMAGE_DIR, fname))
```

---

## 五、评估体系

### 四个指标
**函数**: `metrics.py`

| 指标 | 函数 | 计算方法 |
|------|------|---------|
| 精准度 Precision@K | `precision_at_k()` | 检索结果中命中关键词的比例 |
| 召回率 Recall | `recall_crude()` | 关键词被检索覆盖的比例 |
| 忠实度 Faithfulness | `faithfulness()` | LLM 逐句判断回答是否基于来源（0~1分） |
| 答案相关性 | `answer_relevance()` | 问题与回答的 Embedding 余弦相似度 |

### 自动评估
**函数**: `metrics.py:298 evaluate_rag_auto()`
```python
# 从知识库随机采样 → LLM 反向生成问题 → 用生成的问题检索 → 检查原块是否被召回
test_cases = generate_test_cases(num_cases=5)
return evaluate_rag(test_cases)
```

---

## 六、关键优化点

| 优化 | 方案 | 效果 |
|------|------|------|
| 图片描述并行化 | ThreadPoolExecutor(3线程) | 3倍加速（800张图从67分钟到22分钟） |
| 图片描述可关闭 | IMAGE_DESCRIBE=false | 大文档跳过API调用 |
| 对话缓存 | Embedding相似度匹配(>85%) | 重复问题秒回，零API消耗 |
| Agent 防死循环 | System Prompt 精简 + recursion_limit 25 | 消除无限重试 |
| MinerU 替代 pymupdf | Docker 调用版面分析模型 | 准确率大幅提升，图片自动分类 |
| 大文档超时 | 前端轮询 MAX_POLLS=2400(20分钟) | 286页文档可正常处理 |

---

## 七、配置管理

所有敏感信息和可调参数通过 `.env` 文件管理：
```ini
DEEPSEEK_API_KEY=sk-xxx          # LLM API密钥
DEEPSEEK_MODEL=deepseek-v4-pro   # LLM模型
DASHSCOPE_API_KEY=sk-xxx         # Embedding + 视觉模型
MYSQL_PASSWORD=your-password     # 数据库密码
MINERU_BACKEND=pipeline          # MinerU后端(pipeline/vllm)
IMAGE_DESCRIBE=true              # 是否启用图片描述
MAX_CHUNKS_PER_DOC=5000          # 单文档最大文本块数
MAX_IMAGES_PER_DOC=1000          # 单文档最大图片数
TOP_K=5                          # 检索返回数
```

---

## 八、问答示例（预期演示效果）

**Q**: 介绍一下 CH32V103 的总线架构  
**Agent 思考**: 调用 check_history → 未命中 → 调用 search_knowledge_base("总线架构")  
**Agent 回答**: 基于来源详细描述 I-Code、D-Code、System Bus、DMA Bus、总线矩阵等结构  
**前端展示**: 思考过程折叠 + 回答正文 + 引用来源卡片(含文件名/相关度/具体文本) + 架构框图内嵌展示
