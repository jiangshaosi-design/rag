# Core
python-dotenv>=1.0.0
pydantic>=2.0.0          # src/api/server.py 的请求模型

# LangChain
langchain>=1.0.0
langchain-openai>=1.0.0
langchain-text-splitters>=1.0.0

# Vector Store
faiss-cpu>=1.8.0

# Database
mysql-connector-python>=8.0.0

# Document Processing
# PDF 由 Docker MinerU 解析（见 Dockerfile.mineru），无需本地 PDF 库
python-docx>=1.1.0       # DOCX 段落 + 表格提取

# Embedding + 视觉
dashscope>=1.20.0        # 阿里云 DashScope (text-embedding-v4 / qwen-vl-plus)

# UI
streamlit>=1.28.0

# HTTP 服务（HTML 版前端）
fastapi>=0.110.0
uvicorn>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9  # FastAPI 文件上传依赖

# Utilities
numpy>=1.24.0
